// Exemplos de uso incorreto de ferramentas destinadas apenas para a utilização de arrays.

let exemploNulo;
let exemploNum;

//console.log(exemploNulo.pop());
console.log(exemploNulo.length);

console.log(exemploNum.pop());
console.log(exemploNulo.length);
